'use strict';

// Define the `phoneDetail` module
angular.module('phoneDetail', [
  'ngRoute'
   /* 
      Shouldn't phoneDetail require `'core'` more explicitly for 
      best practice, since it's leveraging checkmarkFilter? Hmm...
   */ 
]);
